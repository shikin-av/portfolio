import common from './common'

export default {
    protocol: common.protocol,
    host:     common.host,
    port:     common.port,    
    assetsPath:         common.assetsPath,
    apiPath: '/admin/api',
}